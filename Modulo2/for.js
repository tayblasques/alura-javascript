const numeros = [100, 200, 300, 400, 500, 600]

// i recebe 0 como valor incial
// valida se i é menor que o tamanho do array
// incrementa 1 ao valor atual de i
for (let i = 0; i < numeros.length; i++){
    console.log(i,numeros[i])
}